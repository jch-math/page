---
interface Props {
  index: string;
  title: string;
  description: string;
}

const { index, title, description } = Astro.props;
---

<header class="page-header">
  <p class="section-index">{index}</p>
  <h1>{title}</h1>
  <p>{description}</p>
</header>
